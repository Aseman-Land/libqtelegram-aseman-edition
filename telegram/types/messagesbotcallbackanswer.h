// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#ifndef LQTG_TYPE_MESSAGESBOTCALLBACKANSWER
#define LQTG_TYPE_MESSAGESBOTCALLBACKANSWER

#include "telegramtypeobject.h"

#include <QMetaType>
#include <QVariant>
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

#include <QtGlobal>
#include <QString>

class LIBQTELEGRAMSHARED_EXPORT MessagesBotCallbackAnswer : public TelegramTypeObject
{
public:
    enum MessagesBotCallbackAnswerClassType {
        typeMessagesBotCallbackAnswer = 0x1264f1c6
    };

    MessagesBotCallbackAnswer(MessagesBotCallbackAnswerClassType classType = typeMessagesBotCallbackAnswer, InboundPkt *in = 0);
    MessagesBotCallbackAnswer(InboundPkt *in);
    MessagesBotCallbackAnswer(const Null&);
    virtual ~MessagesBotCallbackAnswer();

    void setAlert(bool alert);
    bool alert() const;

    void setFlags(qint32 flags);
    qint32 flags() const;

    void setMessage(const QString &message);
    QString message() const;

    void setClassType(MessagesBotCallbackAnswerClassType classType);
    MessagesBotCallbackAnswerClassType classType() const;

    bool fetch(InboundPkt *in);
    bool push(OutboundPkt *out) const;

    QMap<QString, QVariant> toMap() const;
    static MessagesBotCallbackAnswer fromMap(const QMap<QString, QVariant> &map);

    bool operator ==(const MessagesBotCallbackAnswer &b) const;

    bool operator==(bool stt) const { return isNull() != stt; }
    bool operator!=(bool stt) const { return !operator ==(stt); }

    QByteArray getHash(QCryptographicHash::Algorithm alg = QCryptographicHash::Md5) const;

private:
    qint32 m_flags;
    QString m_message;
    MessagesBotCallbackAnswerClassType m_classType;
};

Q_DECLARE_METATYPE(MessagesBotCallbackAnswer)

QDataStream LIBQTELEGRAMSHARED_EXPORT &operator<<(QDataStream &stream, const MessagesBotCallbackAnswer &item);
QDataStream LIBQTELEGRAMSHARED_EXPORT &operator>>(QDataStream &stream, MessagesBotCallbackAnswer &item);

inline MessagesBotCallbackAnswer::MessagesBotCallbackAnswer(MessagesBotCallbackAnswerClassType classType, InboundPkt *in) :
    m_flags(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

inline MessagesBotCallbackAnswer::MessagesBotCallbackAnswer(InboundPkt *in) :
    m_flags(0),
    m_classType(typeMessagesBotCallbackAnswer)
{
    fetch(in);
}

inline MessagesBotCallbackAnswer::MessagesBotCallbackAnswer(const Null &null) :
    TelegramTypeObject(null),
    m_flags(0),
    m_classType(typeMessagesBotCallbackAnswer)
{
}

inline MessagesBotCallbackAnswer::~MessagesBotCallbackAnswer() {
}

inline void MessagesBotCallbackAnswer::setAlert(bool alert) {
    if(alert) m_flags = (m_flags | (1<<1));
    else m_flags = (m_flags & ~(1<<1));
}

inline bool MessagesBotCallbackAnswer::alert() const {
    return (m_flags & 1<<1);
}

inline void MessagesBotCallbackAnswer::setFlags(qint32 flags) {
    m_flags = flags;
}

inline qint32 MessagesBotCallbackAnswer::flags() const {
    return m_flags;
}

inline void MessagesBotCallbackAnswer::setMessage(const QString &message) {
    m_message = message;
}

inline QString MessagesBotCallbackAnswer::message() const {
    return m_message;
}

inline bool MessagesBotCallbackAnswer::operator ==(const MessagesBotCallbackAnswer &b) const {
    return m_classType == b.m_classType &&
           m_flags == b.m_flags &&
           m_message == b.m_message;
}

inline void MessagesBotCallbackAnswer::setClassType(MessagesBotCallbackAnswer::MessagesBotCallbackAnswerClassType classType) {
    m_classType = classType;
}

inline MessagesBotCallbackAnswer::MessagesBotCallbackAnswerClassType MessagesBotCallbackAnswer::classType() const {
    return m_classType;
}

inline bool MessagesBotCallbackAnswer::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeMessagesBotCallbackAnswer: {
        m_flags = in->fetchInt();
        if(m_flags & 1<<0) {
            m_message = in->fetchQString();
        }
        m_classType = static_cast<MessagesBotCallbackAnswerClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

inline bool MessagesBotCallbackAnswer::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeMessagesBotCallbackAnswer: {
        out->appendInt(m_flags);
        out->appendQString(m_message);
        return true;
    }
        break;
    
    default:
        return false;
    }
}

inline QMap<QString, QVariant> MessagesBotCallbackAnswer::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeMessagesBotCallbackAnswer: {
        result["classType"] = "MessagesBotCallbackAnswer::typeMessagesBotCallbackAnswer";
        result["alert"] = QVariant::fromValue<bool>(alert());
        result["message"] = QVariant::fromValue<QString>(message());
        return result;
    }
        break;
    
    default:
        return result;
    }
}

inline MessagesBotCallbackAnswer MessagesBotCallbackAnswer::fromMap(const QMap<QString, QVariant> &map) {
    MessagesBotCallbackAnswer result;
    if(map.value("classType").toString() == "MessagesBotCallbackAnswer::typeMessagesBotCallbackAnswer") {
        result.setClassType(typeMessagesBotCallbackAnswer);
        result.setAlert( map.value("alert").value<bool>() );
        result.setMessage( map.value("message").value<QString>() );
        return result;
    }
    return result;
}

inline QByteArray MessagesBotCallbackAnswer::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

inline QDataStream &operator<<(QDataStream &stream, const MessagesBotCallbackAnswer &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case MessagesBotCallbackAnswer::typeMessagesBotCallbackAnswer:
        stream << item.flags();
        stream << item.message();
        break;
    }
    return stream;
}

inline QDataStream &operator>>(QDataStream &stream, MessagesBotCallbackAnswer &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<MessagesBotCallbackAnswer::MessagesBotCallbackAnswerClassType>(type));
    switch(type) {
    case MessagesBotCallbackAnswer::typeMessagesBotCallbackAnswer: {
        qint32 m_flags;
        stream >> m_flags;
        item.setFlags(m_flags);
        QString m_message;
        stream >> m_message;
        item.setMessage(m_message);
    }
        break;
    }
    return stream;
}


#endif // LQTG_TYPE_MESSAGESBOTCALLBACKANSWER
