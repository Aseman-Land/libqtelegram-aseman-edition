// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "contactsresolvedpeer.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

ContactsResolvedPeer::ContactsResolvedPeer(ContactsResolvedPeerClassType classType, InboundPkt *in) :
    m_classType(classType)
{
    if(in) fetch(in);
}

ContactsResolvedPeer::ContactsResolvedPeer(InboundPkt *in) :
    m_classType(typeContactsResolvedPeer)
{
    fetch(in);
}

ContactsResolvedPeer::ContactsResolvedPeer(const Null &null) :
    TelegramTypeObject(null),
    m_classType(typeContactsResolvedPeer)
{
}

ContactsResolvedPeer::~ContactsResolvedPeer() {
}

void ContactsResolvedPeer::setChats(const QList<Chat> &chats) {
    m_chats = chats;
}

QList<Chat> ContactsResolvedPeer::chats() const {
    return m_chats;
}

void ContactsResolvedPeer::setPeer(const Peer &peer) {
    m_peer = peer;
}

Peer ContactsResolvedPeer::peer() const {
    return m_peer;
}

void ContactsResolvedPeer::setUsers(const QList<User> &users) {
    m_users = users;
}

QList<User> ContactsResolvedPeer::users() const {
    return m_users;
}

bool ContactsResolvedPeer::operator ==(const ContactsResolvedPeer &b) const {
    return m_classType == b.m_classType &&
           m_chats == b.m_chats &&
           m_peer == b.m_peer &&
           m_users == b.m_users;
}

void ContactsResolvedPeer::setClassType(ContactsResolvedPeer::ContactsResolvedPeerClassType classType) {
    m_classType = classType;
}

ContactsResolvedPeer::ContactsResolvedPeerClassType ContactsResolvedPeer::classType() const {
    return m_classType;
}

bool ContactsResolvedPeer::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeContactsResolvedPeer: {
        m_peer.fetch(in);
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_chats_length = in->fetchInt();
        m_chats.clear();
        for (qint32 i = 0; i < m_chats_length; i++) {
            Chat type;
            type.fetch(in);
            m_chats.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_classType = static_cast<ContactsResolvedPeerClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool ContactsResolvedPeer::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeContactsResolvedPeer: {
        m_peer.push(out);
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_chats.count());
        for (qint32 i = 0; i < m_chats.count(); i++) {
            m_chats[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        return true;
    }
        break;
    
    default:
        return false;
    }
}

QMap<QString, QVariant> ContactsResolvedPeer::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeContactsResolvedPeer: {
        result["classType"] = "ContactsResolvedPeer::typeContactsResolvedPeer";
        result["peer"] = m_peer.toMap();
        QList<QVariant> _chats;
        Q_FOREACH(const Chat &m__type, m_chats)
            _chats << m__type.toMap();
        result["chats"] = _chats;
        QList<QVariant> _users;
        Q_FOREACH(const User &m__type, m_users)
            _users << m__type.toMap();
        result["users"] = _users;
        return result;
    }
        break;
    
    default:
        return result;
    }
}

ContactsResolvedPeer ContactsResolvedPeer::fromMap(const QMap<QString, QVariant> &map) {
    ContactsResolvedPeer result;
    if(map.value("classType").toString() == "ContactsResolvedPeer::typeContactsResolvedPeer") {
        result.setClassType(typeContactsResolvedPeer);
        result.setPeer( Peer::fromMap(map.value("peer").toMap()) );
        QList<QVariant> map_chats = map["chats"].toList();
        QList<Chat> _chats;
        Q_FOREACH(const QVariant &var, map_chats)
            _chats << Chat::fromMap(var.toMap());
        result.setChats(_chats);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        Q_FOREACH(const QVariant &var, map_users)
            _users << User::fromMap(var.toMap());
        result.setUsers(_users);
        return result;
    }
    return result;
}

QByteArray ContactsResolvedPeer::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const ContactsResolvedPeer &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case ContactsResolvedPeer::typeContactsResolvedPeer:
        stream << item.peer();
        stream << item.chats();
        stream << item.users();
        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, ContactsResolvedPeer &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<ContactsResolvedPeer::ContactsResolvedPeerClassType>(type));
    switch(type) {
    case ContactsResolvedPeer::typeContactsResolvedPeer: {
        Peer m_peer;
        stream >> m_peer;
        item.setPeer(m_peer);
        QList<Chat> m_chats;
        stream >> m_chats;
        item.setChats(m_chats);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
    }
        break;
    }
    return stream;
}

