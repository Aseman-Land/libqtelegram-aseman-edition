// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "photo.h"

using namespace Tg;
using namespace Types;

Types::Photo::Photo(PhotoType classType, InboundPkt *in) :
    m_accessHash(0),
    m_date(0),
    m_id(0),
    m_userId(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

Types::Photo::Photo(InboundPkt *in) :
    m_accessHash(0),
    m_date(0),
    m_id(0),
    m_userId(0),
    m_classType(typePhotoEmpty)
{
    fetch(in);
}

void Types::Photo::setAccessHash(qint64 accessHash) {
    m_accessHash = accessHash;
}

qint64 Types::Photo::accessHash() const {
    return m_accessHash;
}

void Types::Photo::setDate(qint32 date) {
    m_date = date;
}

qint32 Types::Photo::date() const {
    return m_date;
}

void Types::Photo::setGeo(const GeoPoint &geo) {
    m_geo = geo;
}

GeoPoint Types::Photo::geo() const {
    return m_geo;
}

void Types::Photo::setId(qint64 id) {
    m_id = id;
}

qint64 Types::Photo::id() const {
    return m_id;
}

void Types::Photo::setSizes(const QList<PhotoSize> &sizes) {
    m_sizes = sizes;
}

QList<PhotoSize> Types::Photo::sizes() const {
    return m_sizes;
}

void Types::Photo::setUserId(qint32 userId) {
    m_userId = userId;
}

qint32 Types::Photo::userId() const {
    return m_userId;
}

bool Types::Photo::operator ==(const Photo &b) {
    return m_accessHash == b.m_accessHash &&
           m_date == b.m_date &&
           m_geo == b.m_geo &&
           m_id == b.m_id &&
           m_sizes == b.m_sizes &&
           m_userId == b.m_userId;
}

void Types::Photo::setClassType(Photo::PhotoType classType) {
    m_classType = classType;
}

Types::Photo::PhotoType Types::Photo::classType() const {
    return m_classType;
}

bool Types::Photo::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typePhotoEmpty: {
        m_id = in->fetchLong();
        m_classType = static_cast<PhotoType>(x);
        return true;
    }
        break;
    
    case typePhoto: {
        m_id = in->fetchLong();
        m_accessHash = in->fetchLong();
        m_userId = in->fetchInt();
        m_date = in->fetchInt();
        m_geo.fetch(in);
        if(in->fetchInt() != (qint32)TL_Vector) return false;
        qint32 m_sizes_length = in->fetchInt();
        m_sizes.clear();
        for (qint32 i = 0; i < m_sizes_length; i++) {
            PhotoSize type;
            type.fetch(in);
            m_sizes.append(type);
        }
        m_classType = static_cast<PhotoType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool Types::Photo::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typePhotoEmpty: {
        out->appendLong(m_id);
        return true;
    }
        break;
    
    case typePhoto: {
        out->appendLong(m_id);
        out->appendLong(m_accessHash);
        out->appendInt(m_userId);
        out->appendInt(m_date);
        m_geo.push(out);
        out->appendInt(TL_Vector);
        out->appendInt(m_sizes.count());
        for (qint32 i = 0; i < m_sizes.count(); i++) {
            m_sizes[i].push(out);
        }
        return true;
    }
        break;
    
    default:
        return false;
    }
}

