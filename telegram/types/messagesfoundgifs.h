// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#ifndef LQTG_TYPE_MESSAGESFOUNDGIFS
#define LQTG_TYPE_MESSAGESFOUNDGIFS

#include "telegramtypeobject.h"

#include <QMetaType>
#include <QVariant>
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

#include <QtGlobal>
#include <QList>
#include "foundgif.h"

class LIBQTELEGRAMSHARED_EXPORT MessagesFoundGifs : public TelegramTypeObject
{
public:
    enum MessagesFoundGifsClassType {
        typeMessagesFoundGifs = 0x450a1c0a
    };

    MessagesFoundGifs(MessagesFoundGifsClassType classType = typeMessagesFoundGifs, InboundPkt *in = 0);
    MessagesFoundGifs(InboundPkt *in);
    MessagesFoundGifs(const Null&);
    virtual ~MessagesFoundGifs();

    void setNextOffset(qint32 nextOffset);
    qint32 nextOffset() const;

    void setResults(const QList<FoundGif> &results);
    QList<FoundGif> results() const;

    void setClassType(MessagesFoundGifsClassType classType);
    MessagesFoundGifsClassType classType() const;

    bool fetch(InboundPkt *in);
    bool push(OutboundPkt *out) const;

    QMap<QString, QVariant> toMap() const;
    static MessagesFoundGifs fromMap(const QMap<QString, QVariant> &map);

    bool operator ==(const MessagesFoundGifs &b) const;

    bool operator==(bool stt) const { return isNull() != stt; }
    bool operator!=(bool stt) const { return !operator ==(stt); }

    QByteArray getHash(QCryptographicHash::Algorithm alg = QCryptographicHash::Md5) const;

private:
    qint32 m_nextOffset;
    QList<FoundGif> m_results;
    MessagesFoundGifsClassType m_classType;
};

Q_DECLARE_METATYPE(MessagesFoundGifs)

QDataStream LIBQTELEGRAMSHARED_EXPORT &operator<<(QDataStream &stream, const MessagesFoundGifs &item);
QDataStream LIBQTELEGRAMSHARED_EXPORT &operator>>(QDataStream &stream, MessagesFoundGifs &item);

inline MessagesFoundGifs::MessagesFoundGifs(MessagesFoundGifsClassType classType, InboundPkt *in) :
    m_nextOffset(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

inline MessagesFoundGifs::MessagesFoundGifs(InboundPkt *in) :
    m_nextOffset(0),
    m_classType(typeMessagesFoundGifs)
{
    fetch(in);
}

inline MessagesFoundGifs::MessagesFoundGifs(const Null &null) :
    TelegramTypeObject(null),
    m_nextOffset(0),
    m_classType(typeMessagesFoundGifs)
{
}

inline MessagesFoundGifs::~MessagesFoundGifs() {
}

inline void MessagesFoundGifs::setNextOffset(qint32 nextOffset) {
    m_nextOffset = nextOffset;
}

inline qint32 MessagesFoundGifs::nextOffset() const {
    return m_nextOffset;
}

inline void MessagesFoundGifs::setResults(const QList<FoundGif> &results) {
    m_results = results;
}

inline QList<FoundGif> MessagesFoundGifs::results() const {
    return m_results;
}

inline bool MessagesFoundGifs::operator ==(const MessagesFoundGifs &b) const {
    return m_classType == b.m_classType &&
           m_nextOffset == b.m_nextOffset &&
           m_results == b.m_results;
}

inline void MessagesFoundGifs::setClassType(MessagesFoundGifs::MessagesFoundGifsClassType classType) {
    m_classType = classType;
}

inline MessagesFoundGifs::MessagesFoundGifsClassType MessagesFoundGifs::classType() const {
    return m_classType;
}

inline bool MessagesFoundGifs::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeMessagesFoundGifs: {
        m_nextOffset = in->fetchInt();
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_results_length = in->fetchInt();
        m_results.clear();
        for (qint32 i = 0; i < m_results_length; i++) {
            FoundGif type;
            type.fetch(in);
            m_results.append(type);
        }
        m_classType = static_cast<MessagesFoundGifsClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

inline bool MessagesFoundGifs::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeMessagesFoundGifs: {
        out->appendInt(m_nextOffset);
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_results.count());
        for (qint32 i = 0; i < m_results.count(); i++) {
            m_results[i].push(out);
        }
        return true;
    }
        break;
    
    default:
        return false;
    }
}

inline QMap<QString, QVariant> MessagesFoundGifs::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeMessagesFoundGifs: {
        result["classType"] = "MessagesFoundGifs::typeMessagesFoundGifs";
        result["nextOffset"] = QVariant::fromValue<qint32>(nextOffset());
        QList<QVariant> _results;
        Q_FOREACH(const FoundGif &m__type, m_results)
            _results << m__type.toMap();
        result["results"] = _results;
        return result;
    }
        break;
    
    default:
        return result;
    }
}

inline MessagesFoundGifs MessagesFoundGifs::fromMap(const QMap<QString, QVariant> &map) {
    MessagesFoundGifs result;
    if(map.value("classType").toString() == "MessagesFoundGifs::typeMessagesFoundGifs") {
        result.setClassType(typeMessagesFoundGifs);
        result.setNextOffset( map.value("nextOffset").value<qint32>() );
        QList<QVariant> map_results = map["results"].toList();
        QList<FoundGif> _results;
        Q_FOREACH(const QVariant &var, map_results)
            _results << FoundGif::fromMap(var.toMap());
        result.setResults(_results);
        return result;
    }
    return result;
}

inline QByteArray MessagesFoundGifs::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

inline QDataStream &operator<<(QDataStream &stream, const MessagesFoundGifs &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case MessagesFoundGifs::typeMessagesFoundGifs:
        stream << item.nextOffset();
        stream << item.results();
        break;
    }
    return stream;
}

inline QDataStream &operator>>(QDataStream &stream, MessagesFoundGifs &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<MessagesFoundGifs::MessagesFoundGifsClassType>(type));
    switch(type) {
    case MessagesFoundGifs::typeMessagesFoundGifs: {
        qint32 m_next_offset;
        stream >> m_next_offset;
        item.setNextOffset(m_next_offset);
        QList<FoundGif> m_results;
        stream >> m_results;
        item.setResults(m_results);
    }
        break;
    }
    return stream;
}


#endif // LQTG_TYPE_MESSAGESFOUNDGIFS
