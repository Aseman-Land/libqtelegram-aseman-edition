// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#ifndef LQTG_TYPE_CONTACTSBLOCKED
#define LQTG_TYPE_CONTACTSBLOCKED

#include "telegramtypeobject.h"

#include <QMetaType>
#include <QVariant>
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

#include <QList>
#include "contactblocked.h"
#include <QtGlobal>
#include "user.h"

class LIBQTELEGRAMSHARED_EXPORT ContactsBlocked : public TelegramTypeObject
{
public:
    enum ContactsBlockedClassType {
        typeContactsBlocked = 0x1c138d15,
        typeContactsBlockedSlice = 0x900802a1
    };

    ContactsBlocked(ContactsBlockedClassType classType = typeContactsBlocked, InboundPkt *in = 0);
    ContactsBlocked(InboundPkt *in);
    ContactsBlocked(const Null&);
    virtual ~ContactsBlocked();

    void setBlocked(const QList<ContactBlocked> &blocked);
    QList<ContactBlocked> blocked() const;

    void setCount(qint32 count);
    qint32 count() const;

    void setUsers(const QList<User> &users);
    QList<User> users() const;

    void setClassType(ContactsBlockedClassType classType);
    ContactsBlockedClassType classType() const;

    bool fetch(InboundPkt *in);
    bool push(OutboundPkt *out) const;

    QMap<QString, QVariant> toMap() const;
    static ContactsBlocked fromMap(const QMap<QString, QVariant> &map);

    bool operator ==(const ContactsBlocked &b) const;

    bool operator==(bool stt) const { return isNull() != stt; }
    bool operator!=(bool stt) const { return !operator ==(stt); }

    QByteArray getHash(QCryptographicHash::Algorithm alg = QCryptographicHash::Md5) const;

private:
    QList<ContactBlocked> m_blocked;
    qint32 m_count;
    QList<User> m_users;
    ContactsBlockedClassType m_classType;
};

Q_DECLARE_METATYPE(ContactsBlocked)

QDataStream LIBQTELEGRAMSHARED_EXPORT &operator<<(QDataStream &stream, const ContactsBlocked &item);
QDataStream LIBQTELEGRAMSHARED_EXPORT &operator>>(QDataStream &stream, ContactsBlocked &item);

inline ContactsBlocked::ContactsBlocked(ContactsBlockedClassType classType, InboundPkt *in) :
    m_count(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

inline ContactsBlocked::ContactsBlocked(InboundPkt *in) :
    m_count(0),
    m_classType(typeContactsBlocked)
{
    fetch(in);
}

inline ContactsBlocked::ContactsBlocked(const Null &null) :
    TelegramTypeObject(null),
    m_count(0),
    m_classType(typeContactsBlocked)
{
}

inline ContactsBlocked::~ContactsBlocked() {
}

inline void ContactsBlocked::setBlocked(const QList<ContactBlocked> &blocked) {
    m_blocked = blocked;
}

inline QList<ContactBlocked> ContactsBlocked::blocked() const {
    return m_blocked;
}

inline void ContactsBlocked::setCount(qint32 count) {
    m_count = count;
}

inline qint32 ContactsBlocked::count() const {
    return m_count;
}

inline void ContactsBlocked::setUsers(const QList<User> &users) {
    m_users = users;
}

inline QList<User> ContactsBlocked::users() const {
    return m_users;
}

inline bool ContactsBlocked::operator ==(const ContactsBlocked &b) const {
    return m_classType == b.m_classType &&
           m_blocked == b.m_blocked &&
           m_count == b.m_count &&
           m_users == b.m_users;
}

inline void ContactsBlocked::setClassType(ContactsBlocked::ContactsBlockedClassType classType) {
    m_classType = classType;
}

inline ContactsBlocked::ContactsBlockedClassType ContactsBlocked::classType() const {
    return m_classType;
}

inline bool ContactsBlocked::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeContactsBlocked: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_blocked_length = in->fetchInt();
        m_blocked.clear();
        for (qint32 i = 0; i < m_blocked_length; i++) {
            ContactBlocked type;
            type.fetch(in);
            m_blocked.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_classType = static_cast<ContactsBlockedClassType>(x);
        return true;
    }
        break;
    
    case typeContactsBlockedSlice: {
        m_count = in->fetchInt();
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_blocked_length = in->fetchInt();
        m_blocked.clear();
        for (qint32 i = 0; i < m_blocked_length; i++) {
            ContactBlocked type;
            type.fetch(in);
            m_blocked.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_classType = static_cast<ContactsBlockedClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

inline bool ContactsBlocked::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeContactsBlocked: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_blocked.count());
        for (qint32 i = 0; i < m_blocked.count(); i++) {
            m_blocked[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        return true;
    }
        break;
    
    case typeContactsBlockedSlice: {
        out->appendInt(m_count);
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_blocked.count());
        for (qint32 i = 0; i < m_blocked.count(); i++) {
            m_blocked[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        return true;
    }
        break;
    
    default:
        return false;
    }
}

inline QMap<QString, QVariant> ContactsBlocked::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeContactsBlocked: {
        result["classType"] = "ContactsBlocked::typeContactsBlocked";
        QList<QVariant> _blocked;
        Q_FOREACH(const ContactBlocked &m__type, m_blocked)
            _blocked << m__type.toMap();
        result["blocked"] = _blocked;
        QList<QVariant> _users;
        Q_FOREACH(const User &m__type, m_users)
            _users << m__type.toMap();
        result["users"] = _users;
        return result;
    }
        break;
    
    case typeContactsBlockedSlice: {
        result["classType"] = "ContactsBlocked::typeContactsBlockedSlice";
        result["count"] = QVariant::fromValue<qint32>(count());
        QList<QVariant> _blocked;
        Q_FOREACH(const ContactBlocked &m__type, m_blocked)
            _blocked << m__type.toMap();
        result["blocked"] = _blocked;
        QList<QVariant> _users;
        Q_FOREACH(const User &m__type, m_users)
            _users << m__type.toMap();
        result["users"] = _users;
        return result;
    }
        break;
    
    default:
        return result;
    }
}

inline ContactsBlocked ContactsBlocked::fromMap(const QMap<QString, QVariant> &map) {
    ContactsBlocked result;
    if(map.value("classType").toString() == "ContactsBlocked::typeContactsBlocked") {
        result.setClassType(typeContactsBlocked);
        QList<QVariant> map_blocked = map["blocked"].toList();
        QList<ContactBlocked> _blocked;
        Q_FOREACH(const QVariant &var, map_blocked)
            _blocked << ContactBlocked::fromMap(var.toMap());
        result.setBlocked(_blocked);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        Q_FOREACH(const QVariant &var, map_users)
            _users << User::fromMap(var.toMap());
        result.setUsers(_users);
        return result;
    }
    if(map.value("classType").toString() == "ContactsBlocked::typeContactsBlockedSlice") {
        result.setClassType(typeContactsBlockedSlice);
        result.setCount( map.value("count").value<qint32>() );
        QList<QVariant> map_blocked = map["blocked"].toList();
        QList<ContactBlocked> _blocked;
        Q_FOREACH(const QVariant &var, map_blocked)
            _blocked << ContactBlocked::fromMap(var.toMap());
        result.setBlocked(_blocked);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        Q_FOREACH(const QVariant &var, map_users)
            _users << User::fromMap(var.toMap());
        result.setUsers(_users);
        return result;
    }
    return result;
}

inline QByteArray ContactsBlocked::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

inline QDataStream &operator<<(QDataStream &stream, const ContactsBlocked &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case ContactsBlocked::typeContactsBlocked:
        stream << item.blocked();
        stream << item.users();
        break;
    case ContactsBlocked::typeContactsBlockedSlice:
        stream << item.count();
        stream << item.blocked();
        stream << item.users();
        break;
    }
    return stream;
}

inline QDataStream &operator>>(QDataStream &stream, ContactsBlocked &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<ContactsBlocked::ContactsBlockedClassType>(type));
    switch(type) {
    case ContactsBlocked::typeContactsBlocked: {
        QList<ContactBlocked> m_blocked;
        stream >> m_blocked;
        item.setBlocked(m_blocked);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
    }
        break;
    case ContactsBlocked::typeContactsBlockedSlice: {
        qint32 m_count;
        stream >> m_count;
        item.setCount(m_count);
        QList<ContactBlocked> m_blocked;
        stream >> m_blocked;
        item.setBlocked(m_blocked);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
    }
        break;
    }
    return stream;
}


#endif // LQTG_TYPE_CONTACTSBLOCKED
