// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "document.h"

using namespace Tg;
using namespace Types;

Types::Document::Document(DocumentType classType, InboundPkt *in) :
    m_accessHash(0),
    m_date(0),
    m_dcId(0),
    m_id(0),
    m_size(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

Types::Document::Document(InboundPkt *in) :
    m_accessHash(0),
    m_date(0),
    m_dcId(0),
    m_id(0),
    m_size(0),
    m_classType(typeDocumentEmpty)
{
    fetch(in);
}

void Types::Document::setAccessHash(qint64 accessHash) {
    m_accessHash = accessHash;
}

qint64 Types::Document::accessHash() const {
    return m_accessHash;
}

void Types::Document::setAttributes(const QList<DocumentAttribute> &attributes) {
    m_attributes = attributes;
}

QList<DocumentAttribute> Types::Document::attributes() const {
    return m_attributes;
}

void Types::Document::setDate(qint32 date) {
    m_date = date;
}

qint32 Types::Document::date() const {
    return m_date;
}

void Types::Document::setDcId(qint32 dcId) {
    m_dcId = dcId;
}

qint32 Types::Document::dcId() const {
    return m_dcId;
}

void Types::Document::setId(qint64 id) {
    m_id = id;
}

qint64 Types::Document::id() const {
    return m_id;
}

void Types::Document::setMimeType(const QString &mimeType) {
    m_mimeType = mimeType;
}

QString Types::Document::mimeType() const {
    return m_mimeType;
}

void Types::Document::setSize(qint32 size) {
    m_size = size;
}

qint32 Types::Document::size() const {
    return m_size;
}

void Types::Document::setThumb(const PhotoSize &thumb) {
    m_thumb = thumb;
}

PhotoSize Types::Document::thumb() const {
    return m_thumb;
}

bool Types::Document::operator ==(const Document &b) {
    return m_accessHash == b.m_accessHash &&
           m_attributes == b.m_attributes &&
           m_date == b.m_date &&
           m_dcId == b.m_dcId &&
           m_id == b.m_id &&
           m_mimeType == b.m_mimeType &&
           m_size == b.m_size &&
           m_thumb == b.m_thumb;
}

void Types::Document::setClassType(Document::DocumentType classType) {
    m_classType = classType;
}

Types::Document::DocumentType Types::Document::classType() const {
    return m_classType;
}

bool Types::Document::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeDocumentEmpty: {
        m_id = in->fetchLong();
        m_classType = static_cast<DocumentType>(x);
        return true;
    }
        break;
    
    case typeDocument: {
        m_id = in->fetchLong();
        m_accessHash = in->fetchLong();
        m_date = in->fetchInt();
        m_mimeType = in->fetchQString();
        m_size = in->fetchInt();
        m_thumb.fetch(in);
        m_dcId = in->fetchInt();
        if(in->fetchInt() != (qint32)TL_Vector) return false;
        qint32 m_attributes_length = in->fetchInt();
        m_attributes.clear();
        for (qint32 i = 0; i < m_attributes_length; i++) {
            DocumentAttribute type;
            type.fetch(in);
            m_attributes.append(type);
        }
        m_classType = static_cast<DocumentType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool Types::Document::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeDocumentEmpty: {
        out->appendLong(m_id);
        return true;
    }
        break;
    
    case typeDocument: {
        out->appendLong(m_id);
        out->appendLong(m_accessHash);
        out->appendInt(m_date);
        out->appendQString(m_mimeType);
        out->appendInt(m_size);
        m_thumb.push(out);
        out->appendInt(m_dcId);
        out->appendInt(TL_Vector);
        out->appendInt(m_attributes.count());
        for (qint32 i = 0; i < m_attributes.count(); i++) {
            m_attributes[i].push(out);
        }
        return true;
    }
        break;
    
    default:
        return false;
    }
}

