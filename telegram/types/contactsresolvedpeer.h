// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#ifndef LQTG_TYPE_CONTACTSRESOLVEDPEER
#define LQTG_TYPE_CONTACTSRESOLVEDPEER

#include "telegramtypeobject.h"

#include <QMetaType>
#include <QVariant>
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

#include <QList>
#include "chat.h"
#include "peer.h"
#include "user.h"

class LIBQTELEGRAMSHARED_EXPORT ContactsResolvedPeer : public TelegramTypeObject
{
public:
    enum ContactsResolvedPeerClassType {
        typeContactsResolvedPeer = 0x7f077ad9
    };

    ContactsResolvedPeer(ContactsResolvedPeerClassType classType = typeContactsResolvedPeer, InboundPkt *in = 0);
    ContactsResolvedPeer(InboundPkt *in);
    ContactsResolvedPeer(const Null&);
    virtual ~ContactsResolvedPeer();

    void setChats(const QList<Chat> &chats);
    QList<Chat> chats() const;

    void setPeer(const Peer &peer);
    Peer peer() const;

    void setUsers(const QList<User> &users);
    QList<User> users() const;

    void setClassType(ContactsResolvedPeerClassType classType);
    ContactsResolvedPeerClassType classType() const;

    bool fetch(InboundPkt *in);
    bool push(OutboundPkt *out) const;

    QMap<QString, QVariant> toMap() const;
    static ContactsResolvedPeer fromMap(const QMap<QString, QVariant> &map);

    bool operator ==(const ContactsResolvedPeer &b) const;

    bool operator==(bool stt) const { return isNull() != stt; }
    bool operator!=(bool stt) const { return !operator ==(stt); }

    QByteArray getHash(QCryptographicHash::Algorithm alg = QCryptographicHash::Md5) const;

private:
    QList<Chat> m_chats;
    Peer m_peer;
    QList<User> m_users;
    ContactsResolvedPeerClassType m_classType;
};

Q_DECLARE_METATYPE(ContactsResolvedPeer)

QDataStream LIBQTELEGRAMSHARED_EXPORT &operator<<(QDataStream &stream, const ContactsResolvedPeer &item);
QDataStream LIBQTELEGRAMSHARED_EXPORT &operator>>(QDataStream &stream, ContactsResolvedPeer &item);

inline ContactsResolvedPeer::ContactsResolvedPeer(ContactsResolvedPeerClassType classType, InboundPkt *in) :
    m_classType(classType)
{
    if(in) fetch(in);
}

inline ContactsResolvedPeer::ContactsResolvedPeer(InboundPkt *in) :
    m_classType(typeContactsResolvedPeer)
{
    fetch(in);
}

inline ContactsResolvedPeer::ContactsResolvedPeer(const Null &null) :
    TelegramTypeObject(null),
    m_classType(typeContactsResolvedPeer)
{
}

inline ContactsResolvedPeer::~ContactsResolvedPeer() {
}

inline void ContactsResolvedPeer::setChats(const QList<Chat> &chats) {
    m_chats = chats;
}

inline QList<Chat> ContactsResolvedPeer::chats() const {
    return m_chats;
}

inline void ContactsResolvedPeer::setPeer(const Peer &peer) {
    m_peer = peer;
}

inline Peer ContactsResolvedPeer::peer() const {
    return m_peer;
}

inline void ContactsResolvedPeer::setUsers(const QList<User> &users) {
    m_users = users;
}

inline QList<User> ContactsResolvedPeer::users() const {
    return m_users;
}

inline bool ContactsResolvedPeer::operator ==(const ContactsResolvedPeer &b) const {
    return m_classType == b.m_classType &&
           m_chats == b.m_chats &&
           m_peer == b.m_peer &&
           m_users == b.m_users;
}

inline void ContactsResolvedPeer::setClassType(ContactsResolvedPeer::ContactsResolvedPeerClassType classType) {
    m_classType = classType;
}

inline ContactsResolvedPeer::ContactsResolvedPeerClassType ContactsResolvedPeer::classType() const {
    return m_classType;
}

inline bool ContactsResolvedPeer::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeContactsResolvedPeer: {
        m_peer.fetch(in);
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_chats_length = in->fetchInt();
        m_chats.clear();
        for (qint32 i = 0; i < m_chats_length; i++) {
            Chat type;
            type.fetch(in);
            m_chats.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_classType = static_cast<ContactsResolvedPeerClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

inline bool ContactsResolvedPeer::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeContactsResolvedPeer: {
        m_peer.push(out);
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_chats.count());
        for (qint32 i = 0; i < m_chats.count(); i++) {
            m_chats[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        return true;
    }
        break;
    
    default:
        return false;
    }
}

inline QMap<QString, QVariant> ContactsResolvedPeer::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeContactsResolvedPeer: {
        result["classType"] = "ContactsResolvedPeer::typeContactsResolvedPeer";
        result["peer"] = m_peer.toMap();
        QList<QVariant> _chats;
        Q_FOREACH(const Chat &m__type, m_chats)
            _chats << m__type.toMap();
        result["chats"] = _chats;
        QList<QVariant> _users;
        Q_FOREACH(const User &m__type, m_users)
            _users << m__type.toMap();
        result["users"] = _users;
        return result;
    }
        break;
    
    default:
        return result;
    }
}

inline ContactsResolvedPeer ContactsResolvedPeer::fromMap(const QMap<QString, QVariant> &map) {
    ContactsResolvedPeer result;
    if(map.value("classType").toString() == "ContactsResolvedPeer::typeContactsResolvedPeer") {
        result.setClassType(typeContactsResolvedPeer);
        result.setPeer( Peer::fromMap(map.value("peer").toMap()) );
        QList<QVariant> map_chats = map["chats"].toList();
        QList<Chat> _chats;
        Q_FOREACH(const QVariant &var, map_chats)
            _chats << Chat::fromMap(var.toMap());
        result.setChats(_chats);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        Q_FOREACH(const QVariant &var, map_users)
            _users << User::fromMap(var.toMap());
        result.setUsers(_users);
        return result;
    }
    return result;
}

inline QByteArray ContactsResolvedPeer::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

inline QDataStream &operator<<(QDataStream &stream, const ContactsResolvedPeer &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case ContactsResolvedPeer::typeContactsResolvedPeer:
        stream << item.peer();
        stream << item.chats();
        stream << item.users();
        break;
    }
    return stream;
}

inline QDataStream &operator>>(QDataStream &stream, ContactsResolvedPeer &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<ContactsResolvedPeer::ContactsResolvedPeerClassType>(type));
    switch(type) {
    case ContactsResolvedPeer::typeContactsResolvedPeer: {
        Peer m_peer;
        stream >> m_peer;
        item.setPeer(m_peer);
        QList<Chat> m_chats;
        stream >> m_chats;
        item.setChats(m_chats);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
    }
        break;
    }
    return stream;
}


#endif // LQTG_TYPE_CONTACTSRESOLVEDPEER
