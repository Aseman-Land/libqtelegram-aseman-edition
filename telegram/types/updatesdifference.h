// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#ifndef LQTG_TYPE_UPDATESDIFFERENCE
#define LQTG_TYPE_UPDATESDIFFERENCE

#include "telegramtypeobject.h"

#include <QMetaType>
#include <QVariant>
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

#include <QList>
#include "chat.h"
#include <QtGlobal>
#include "updatesstate.h"
#include "encryptedmessage.h"
#include "message.h"
#include "update.h"
#include "user.h"

class LIBQTELEGRAMSHARED_EXPORT UpdatesDifference : public TelegramTypeObject
{
public:
    enum UpdatesDifferenceClassType {
        typeUpdatesDifferenceEmpty = 0x5d75a138,
        typeUpdatesDifference = 0xf49ca0,
        typeUpdatesDifferenceSlice = 0xa8fb1981
    };

    UpdatesDifference(UpdatesDifferenceClassType classType = typeUpdatesDifferenceEmpty, InboundPkt *in = 0);
    UpdatesDifference(InboundPkt *in);
    UpdatesDifference(const Null&);
    virtual ~UpdatesDifference();

    void setChats(const QList<Chat> &chats);
    QList<Chat> chats() const;

    void setDate(qint32 date);
    qint32 date() const;

    void setIntermediateState(const UpdatesState &intermediateState);
    UpdatesState intermediateState() const;

    void setNewEncryptedMessages(const QList<EncryptedMessage> &newEncryptedMessages);
    QList<EncryptedMessage> newEncryptedMessages() const;

    void setNewMessages(const QList<Message> &newMessages);
    QList<Message> newMessages() const;

    void setOtherUpdates(const QList<Update> &otherUpdates);
    QList<Update> otherUpdates() const;

    void setSeq(qint32 seq);
    qint32 seq() const;

    void setState(const UpdatesState &state);
    UpdatesState state() const;

    void setUsers(const QList<User> &users);
    QList<User> users() const;

    void setClassType(UpdatesDifferenceClassType classType);
    UpdatesDifferenceClassType classType() const;

    bool fetch(InboundPkt *in);
    bool push(OutboundPkt *out) const;

    QMap<QString, QVariant> toMap() const;
    static UpdatesDifference fromMap(const QMap<QString, QVariant> &map);

    bool operator ==(const UpdatesDifference &b) const;

    bool operator==(bool stt) const { return isNull() != stt; }
    bool operator!=(bool stt) const { return !operator ==(stt); }

    QByteArray getHash(QCryptographicHash::Algorithm alg = QCryptographicHash::Md5) const;

private:
    QList<Chat> m_chats;
    qint32 m_date;
    UpdatesState m_intermediateState;
    QList<EncryptedMessage> m_newEncryptedMessages;
    QList<Message> m_newMessages;
    QList<Update> m_otherUpdates;
    qint32 m_seq;
    UpdatesState m_state;
    QList<User> m_users;
    UpdatesDifferenceClassType m_classType;
};

Q_DECLARE_METATYPE(UpdatesDifference)

QDataStream LIBQTELEGRAMSHARED_EXPORT &operator<<(QDataStream &stream, const UpdatesDifference &item);
QDataStream LIBQTELEGRAMSHARED_EXPORT &operator>>(QDataStream &stream, UpdatesDifference &item);

inline UpdatesDifference::UpdatesDifference(UpdatesDifferenceClassType classType, InboundPkt *in) :
    m_date(0),
    m_seq(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

inline UpdatesDifference::UpdatesDifference(InboundPkt *in) :
    m_date(0),
    m_seq(0),
    m_classType(typeUpdatesDifferenceEmpty)
{
    fetch(in);
}

inline UpdatesDifference::UpdatesDifference(const Null &null) :
    TelegramTypeObject(null),
    m_date(0),
    m_seq(0),
    m_classType(typeUpdatesDifferenceEmpty)
{
}

inline UpdatesDifference::~UpdatesDifference() {
}

inline void UpdatesDifference::setChats(const QList<Chat> &chats) {
    m_chats = chats;
}

inline QList<Chat> UpdatesDifference::chats() const {
    return m_chats;
}

inline void UpdatesDifference::setDate(qint32 date) {
    m_date = date;
}

inline qint32 UpdatesDifference::date() const {
    return m_date;
}

inline void UpdatesDifference::setIntermediateState(const UpdatesState &intermediateState) {
    m_intermediateState = intermediateState;
}

inline UpdatesState UpdatesDifference::intermediateState() const {
    return m_intermediateState;
}

inline void UpdatesDifference::setNewEncryptedMessages(const QList<EncryptedMessage> &newEncryptedMessages) {
    m_newEncryptedMessages = newEncryptedMessages;
}

inline QList<EncryptedMessage> UpdatesDifference::newEncryptedMessages() const {
    return m_newEncryptedMessages;
}

inline void UpdatesDifference::setNewMessages(const QList<Message> &newMessages) {
    m_newMessages = newMessages;
}

inline QList<Message> UpdatesDifference::newMessages() const {
    return m_newMessages;
}

inline void UpdatesDifference::setOtherUpdates(const QList<Update> &otherUpdates) {
    m_otherUpdates = otherUpdates;
}

inline QList<Update> UpdatesDifference::otherUpdates() const {
    return m_otherUpdates;
}

inline void UpdatesDifference::setSeq(qint32 seq) {
    m_seq = seq;
}

inline qint32 UpdatesDifference::seq() const {
    return m_seq;
}

inline void UpdatesDifference::setState(const UpdatesState &state) {
    m_state = state;
}

inline UpdatesState UpdatesDifference::state() const {
    return m_state;
}

inline void UpdatesDifference::setUsers(const QList<User> &users) {
    m_users = users;
}

inline QList<User> UpdatesDifference::users() const {
    return m_users;
}

inline bool UpdatesDifference::operator ==(const UpdatesDifference &b) const {
    return m_classType == b.m_classType &&
           m_chats == b.m_chats &&
           m_date == b.m_date &&
           m_intermediateState == b.m_intermediateState &&
           m_newEncryptedMessages == b.m_newEncryptedMessages &&
           m_newMessages == b.m_newMessages &&
           m_otherUpdates == b.m_otherUpdates &&
           m_seq == b.m_seq &&
           m_state == b.m_state &&
           m_users == b.m_users;
}

inline void UpdatesDifference::setClassType(UpdatesDifference::UpdatesDifferenceClassType classType) {
    m_classType = classType;
}

inline UpdatesDifference::UpdatesDifferenceClassType UpdatesDifference::classType() const {
    return m_classType;
}

inline bool UpdatesDifference::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeUpdatesDifferenceEmpty: {
        m_date = in->fetchInt();
        m_seq = in->fetchInt();
        m_classType = static_cast<UpdatesDifferenceClassType>(x);
        return true;
    }
        break;
    
    case typeUpdatesDifference: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_newMessages_length = in->fetchInt();
        m_newMessages.clear();
        for (qint32 i = 0; i < m_newMessages_length; i++) {
            Message type;
            type.fetch(in);
            m_newMessages.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_newEncryptedMessages_length = in->fetchInt();
        m_newEncryptedMessages.clear();
        for (qint32 i = 0; i < m_newEncryptedMessages_length; i++) {
            EncryptedMessage type;
            type.fetch(in);
            m_newEncryptedMessages.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_otherUpdates_length = in->fetchInt();
        m_otherUpdates.clear();
        for (qint32 i = 0; i < m_otherUpdates_length; i++) {
            Update type;
            type.fetch(in);
            m_otherUpdates.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_chats_length = in->fetchInt();
        m_chats.clear();
        for (qint32 i = 0; i < m_chats_length; i++) {
            Chat type;
            type.fetch(in);
            m_chats.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_state.fetch(in);
        m_classType = static_cast<UpdatesDifferenceClassType>(x);
        return true;
    }
        break;
    
    case typeUpdatesDifferenceSlice: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_newMessages_length = in->fetchInt();
        m_newMessages.clear();
        for (qint32 i = 0; i < m_newMessages_length; i++) {
            Message type;
            type.fetch(in);
            m_newMessages.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_newEncryptedMessages_length = in->fetchInt();
        m_newEncryptedMessages.clear();
        for (qint32 i = 0; i < m_newEncryptedMessages_length; i++) {
            EncryptedMessage type;
            type.fetch(in);
            m_newEncryptedMessages.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_otherUpdates_length = in->fetchInt();
        m_otherUpdates.clear();
        for (qint32 i = 0; i < m_otherUpdates_length; i++) {
            Update type;
            type.fetch(in);
            m_otherUpdates.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_chats_length = in->fetchInt();
        m_chats.clear();
        for (qint32 i = 0; i < m_chats_length; i++) {
            Chat type;
            type.fetch(in);
            m_chats.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_intermediateState.fetch(in);
        m_classType = static_cast<UpdatesDifferenceClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

inline bool UpdatesDifference::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeUpdatesDifferenceEmpty: {
        out->appendInt(m_date);
        out->appendInt(m_seq);
        return true;
    }
        break;
    
    case typeUpdatesDifference: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_newMessages.count());
        for (qint32 i = 0; i < m_newMessages.count(); i++) {
            m_newMessages[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_newEncryptedMessages.count());
        for (qint32 i = 0; i < m_newEncryptedMessages.count(); i++) {
            m_newEncryptedMessages[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_otherUpdates.count());
        for (qint32 i = 0; i < m_otherUpdates.count(); i++) {
            m_otherUpdates[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_chats.count());
        for (qint32 i = 0; i < m_chats.count(); i++) {
            m_chats[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        m_state.push(out);
        return true;
    }
        break;
    
    case typeUpdatesDifferenceSlice: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_newMessages.count());
        for (qint32 i = 0; i < m_newMessages.count(); i++) {
            m_newMessages[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_newEncryptedMessages.count());
        for (qint32 i = 0; i < m_newEncryptedMessages.count(); i++) {
            m_newEncryptedMessages[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_otherUpdates.count());
        for (qint32 i = 0; i < m_otherUpdates.count(); i++) {
            m_otherUpdates[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_chats.count());
        for (qint32 i = 0; i < m_chats.count(); i++) {
            m_chats[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        m_intermediateState.push(out);
        return true;
    }
        break;
    
    default:
        return false;
    }
}

inline QMap<QString, QVariant> UpdatesDifference::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeUpdatesDifferenceEmpty: {
        result["classType"] = "UpdatesDifference::typeUpdatesDifferenceEmpty";
        result["date"] = QVariant::fromValue<qint32>(date());
        result["seq"] = QVariant::fromValue<qint32>(seq());
        return result;
    }
        break;
    
    case typeUpdatesDifference: {
        result["classType"] = "UpdatesDifference::typeUpdatesDifference";
        QList<QVariant> _newMessages;
        Q_FOREACH(const Message &m__type, m_newMessages)
            _newMessages << m__type.toMap();
        result["newMessages"] = _newMessages;
        QList<QVariant> _newEncryptedMessages;
        Q_FOREACH(const EncryptedMessage &m__type, m_newEncryptedMessages)
            _newEncryptedMessages << m__type.toMap();
        result["newEncryptedMessages"] = _newEncryptedMessages;
        QList<QVariant> _otherUpdates;
        Q_FOREACH(const Update &m__type, m_otherUpdates)
            _otherUpdates << m__type.toMap();
        result["otherUpdates"] = _otherUpdates;
        QList<QVariant> _chats;
        Q_FOREACH(const Chat &m__type, m_chats)
            _chats << m__type.toMap();
        result["chats"] = _chats;
        QList<QVariant> _users;
        Q_FOREACH(const User &m__type, m_users)
            _users << m__type.toMap();
        result["users"] = _users;
        result["state"] = m_state.toMap();
        return result;
    }
        break;
    
    case typeUpdatesDifferenceSlice: {
        result["classType"] = "UpdatesDifference::typeUpdatesDifferenceSlice";
        QList<QVariant> _newMessages;
        Q_FOREACH(const Message &m__type, m_newMessages)
            _newMessages << m__type.toMap();
        result["newMessages"] = _newMessages;
        QList<QVariant> _newEncryptedMessages;
        Q_FOREACH(const EncryptedMessage &m__type, m_newEncryptedMessages)
            _newEncryptedMessages << m__type.toMap();
        result["newEncryptedMessages"] = _newEncryptedMessages;
        QList<QVariant> _otherUpdates;
        Q_FOREACH(const Update &m__type, m_otherUpdates)
            _otherUpdates << m__type.toMap();
        result["otherUpdates"] = _otherUpdates;
        QList<QVariant> _chats;
        Q_FOREACH(const Chat &m__type, m_chats)
            _chats << m__type.toMap();
        result["chats"] = _chats;
        QList<QVariant> _users;
        Q_FOREACH(const User &m__type, m_users)
            _users << m__type.toMap();
        result["users"] = _users;
        result["intermediateState"] = m_intermediateState.toMap();
        return result;
    }
        break;
    
    default:
        return result;
    }
}

inline UpdatesDifference UpdatesDifference::fromMap(const QMap<QString, QVariant> &map) {
    UpdatesDifference result;
    if(map.value("classType").toString() == "UpdatesDifference::typeUpdatesDifferenceEmpty") {
        result.setClassType(typeUpdatesDifferenceEmpty);
        result.setDate( map.value("date").value<qint32>() );
        result.setSeq( map.value("seq").value<qint32>() );
        return result;
    }
    if(map.value("classType").toString() == "UpdatesDifference::typeUpdatesDifference") {
        result.setClassType(typeUpdatesDifference);
        QList<QVariant> map_newMessages = map["newMessages"].toList();
        QList<Message> _newMessages;
        Q_FOREACH(const QVariant &var, map_newMessages)
            _newMessages << Message::fromMap(var.toMap());
        result.setNewMessages(_newMessages);
        QList<QVariant> map_newEncryptedMessages = map["newEncryptedMessages"].toList();
        QList<EncryptedMessage> _newEncryptedMessages;
        Q_FOREACH(const QVariant &var, map_newEncryptedMessages)
            _newEncryptedMessages << EncryptedMessage::fromMap(var.toMap());
        result.setNewEncryptedMessages(_newEncryptedMessages);
        QList<QVariant> map_otherUpdates = map["otherUpdates"].toList();
        QList<Update> _otherUpdates;
        Q_FOREACH(const QVariant &var, map_otherUpdates)
            _otherUpdates << Update::fromMap(var.toMap());
        result.setOtherUpdates(_otherUpdates);
        QList<QVariant> map_chats = map["chats"].toList();
        QList<Chat> _chats;
        Q_FOREACH(const QVariant &var, map_chats)
            _chats << Chat::fromMap(var.toMap());
        result.setChats(_chats);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        Q_FOREACH(const QVariant &var, map_users)
            _users << User::fromMap(var.toMap());
        result.setUsers(_users);
        result.setState( UpdatesState::fromMap(map.value("state").toMap()) );
        return result;
    }
    if(map.value("classType").toString() == "UpdatesDifference::typeUpdatesDifferenceSlice") {
        result.setClassType(typeUpdatesDifferenceSlice);
        QList<QVariant> map_newMessages = map["newMessages"].toList();
        QList<Message> _newMessages;
        Q_FOREACH(const QVariant &var, map_newMessages)
            _newMessages << Message::fromMap(var.toMap());
        result.setNewMessages(_newMessages);
        QList<QVariant> map_newEncryptedMessages = map["newEncryptedMessages"].toList();
        QList<EncryptedMessage> _newEncryptedMessages;
        Q_FOREACH(const QVariant &var, map_newEncryptedMessages)
            _newEncryptedMessages << EncryptedMessage::fromMap(var.toMap());
        result.setNewEncryptedMessages(_newEncryptedMessages);
        QList<QVariant> map_otherUpdates = map["otherUpdates"].toList();
        QList<Update> _otherUpdates;
        Q_FOREACH(const QVariant &var, map_otherUpdates)
            _otherUpdates << Update::fromMap(var.toMap());
        result.setOtherUpdates(_otherUpdates);
        QList<QVariant> map_chats = map["chats"].toList();
        QList<Chat> _chats;
        Q_FOREACH(const QVariant &var, map_chats)
            _chats << Chat::fromMap(var.toMap());
        result.setChats(_chats);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        Q_FOREACH(const QVariant &var, map_users)
            _users << User::fromMap(var.toMap());
        result.setUsers(_users);
        result.setIntermediateState( UpdatesState::fromMap(map.value("intermediateState").toMap()) );
        return result;
    }
    return result;
}

inline QByteArray UpdatesDifference::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

inline QDataStream &operator<<(QDataStream &stream, const UpdatesDifference &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case UpdatesDifference::typeUpdatesDifferenceEmpty:
        stream << item.date();
        stream << item.seq();
        break;
    case UpdatesDifference::typeUpdatesDifference:
        stream << item.newMessages();
        stream << item.newEncryptedMessages();
        stream << item.otherUpdates();
        stream << item.chats();
        stream << item.users();
        stream << item.state();
        break;
    case UpdatesDifference::typeUpdatesDifferenceSlice:
        stream << item.newMessages();
        stream << item.newEncryptedMessages();
        stream << item.otherUpdates();
        stream << item.chats();
        stream << item.users();
        stream << item.intermediateState();
        break;
    }
    return stream;
}

inline QDataStream &operator>>(QDataStream &stream, UpdatesDifference &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<UpdatesDifference::UpdatesDifferenceClassType>(type));
    switch(type) {
    case UpdatesDifference::typeUpdatesDifferenceEmpty: {
        qint32 m_date;
        stream >> m_date;
        item.setDate(m_date);
        qint32 m_seq;
        stream >> m_seq;
        item.setSeq(m_seq);
    }
        break;
    case UpdatesDifference::typeUpdatesDifference: {
        QList<Message> m_new_messages;
        stream >> m_new_messages;
        item.setNewMessages(m_new_messages);
        QList<EncryptedMessage> m_new_encrypted_messages;
        stream >> m_new_encrypted_messages;
        item.setNewEncryptedMessages(m_new_encrypted_messages);
        QList<Update> m_other_updates;
        stream >> m_other_updates;
        item.setOtherUpdates(m_other_updates);
        QList<Chat> m_chats;
        stream >> m_chats;
        item.setChats(m_chats);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
        UpdatesState m_state;
        stream >> m_state;
        item.setState(m_state);
    }
        break;
    case UpdatesDifference::typeUpdatesDifferenceSlice: {
        QList<Message> m_new_messages;
        stream >> m_new_messages;
        item.setNewMessages(m_new_messages);
        QList<EncryptedMessage> m_new_encrypted_messages;
        stream >> m_new_encrypted_messages;
        item.setNewEncryptedMessages(m_new_encrypted_messages);
        QList<Update> m_other_updates;
        stream >> m_other_updates;
        item.setOtherUpdates(m_other_updates);
        QList<Chat> m_chats;
        stream >> m_chats;
        item.setChats(m_chats);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
        UpdatesState m_intermediate_state;
        stream >> m_intermediate_state;
        item.setIntermediateState(m_intermediate_state);
    }
        break;
    }
    return stream;
}


#endif // LQTG_TYPE_UPDATESDIFFERENCE
